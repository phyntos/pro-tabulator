import DevTabulator from './DevTabulator';
import ReactDOM from 'react-dom';
import React from 'react';
import '../index.css';

const element = document.getElementById('root');
ReactDOM.render(<DevTabulator />, element);
